import { Typography } from "@mui/material";
import React from "react";
import "./Header.css";

export default function Header() {
  return (
    <header className="App-header">
      <Typography variant="h1">Dunch Creative:</Typography>
      <Typography variant="h2">Bringing the Arts and Tech together</Typography>
    </header>
  );
}
